# brahma.wheel
Plugin for Brahma for listen mousewheel

# Usage
```javascript
$("#myelement").component('wheel')
.bind('up', function() {
  // Action up
})
.bind('down', function() {
  // Action down
});
```
